/**
 * @description obtiene el scrappig de youtube
 * @param {*} req 
 * @param {*} response 
 */
const getScrappig = async (req, response) => {
    try {
        
    } catch (error) {
        console.error(error);
        
    }
}

module.exports = { getScrappig };
